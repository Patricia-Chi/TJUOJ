# 2026-05-19 后端知识分享会 算法

**后端组迟雯轩 2025本 人工智能学院**

## 1.模板

```cpp
#include <bits/stdc++.h>
#define ll long long
#define ull unsigned long long
#define endl '\n'
// #define int long long // 不开 long long 见祖宗

using namespace std;

using vi = vector<int>;
using vii = vector<vector<int>>;
using pii = pair<int, int>;

const double PI = 3.141592653589793;
const int mod = 998244353;
constexpr int N = -1;
int T;
int n, m, k;
ll ans;
string s;

void solve()
{
}

int main() //signed main
{
    // freopen("test.in", "r", stdin);
    // freopen("test.out", "w", stdout);
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);
    T = 1;
    // cin >> T;
    while (T--)
    {
        solve();
    }
    // fclose(stdin);
    // fclose(stdout);
    return 0;
}
```

---

## 2.复杂度

| 运算量   | n! | 2^n | n^3 |  n^2 |    nlogn |     n√n |    n |  logn |
| -------- | -: | --: | --: | ---: | -------: | -------: | ---: | ----: |
| 最大规模 | 11 |  25 | 300 | 5000 | 5 * 10^5 | 5 * 10^4 | 10^8 | 10^18 |

---

## 3. 基础算法

### 3.1 枚举

适用于数据范围较小，或者答案结构简单，直接尝试所有情况。

### 3.2 模拟

按题意一步一步做，注意剪枝降复杂度

### 3.3 排序

sort默认升序：

```cpp
sort(a.begin(), a.end());
```

降序：

```cpp
sort(a.begin(), a.end(), greater<int>());
```

自定义cmp：

```cpp
bool cmp(int a, int b)
{
    return a > b;
}

sort(a.begin(), a.end(), cmp);
```

重载运算符

### 3.4 贪心

是否满足当前最优不影响后续最优

### 3.5双指针

适用于区间满足某种单调性质

### 3.6前缀和

`pre[i] = a[1] + a[2] + ... + a[i]`

快速求区间和，区间 `[l, r]` 的和为 `pre[r] - pre[l - 1]`

### 3.7差分

适用于频繁区间修改、最后统一还原。

`diff[i] = a[i] - a[i - 1]`

区间加法：

对 `[l, r]` 加 `x`等价于 `diff[l] += x`，`diff[r + 1] -= x`

最后求前缀和恢复原数组。

### 3.8离散化

当数值范围很大，但真正出现的值很少时，把值压缩到连续下标。

收集所有值，排序去重，用下标代替原值

常见用途：

- 树状数组
- 线段树
- 坐标范围很大但点很少的问题

### 3.9树状数组

一种支持 **单点修改** 和 **区间查询** 的，代码量小的数据结构

洛谷P3374 【模板】树状数组 1

已知一个数列，你需要进行下面两种操作：

* 将某一个数加上 **x**；
* 求出某区间每一个数的和。

```cpp
int lowbit(int x)
{
    return x & (-x);
}

int query(int x, int y)
{
    int ans = 0;
    for (int i = y; i > 0; i -= lowbit(i))
    {
        ans += a[i];
    }
    for (int i = x - 1; i > 0; i -= lowbit(i))
    {
        ans -= a[i];
    }
    return ans;
}

void update(int x, int y)
{
    for (int i = x; i <= n; i += lowbit(i))
    {
        a[i] += y;
    }
}

void solve()
{
    cin >> n >> m;
    int d;
    for (int i = 1; i <= n; i++)
    {
        cin >> d;
        update(i, d);
    }
    int op, x, y;
    for (int i = 1; i <= m; i++)
    {
        cin >> op >> x >> y;
        if (op == 1)
        {
            update(x, y);
        }
        else
        {
            cout << query(x, y) << endl;
        }
    }
}
```

## 4.STL 常用容器

### `vector`

动态数组

```cpp
vector<int> a;

a.push_back(3);       // 插入元素
a.pop_back();         // 删除最后一个元素

cout << a.size() << endl;   // 长度
cout << a.empty() << endl;  // 是否为空

for (int i = 0; i < a.size(); i++)
    cout << a[i] << " ";
cout << endl;
```

初始化

```cpp
vector<int> a(10);          // 10 个元素，默认 0
vector<int> b(10, -1);      // 10 个元素，全部是 -1
vector<vector<int>> g(n + 1);
```

排序与去重

```cpp
sort(a.begin(), a.end());

a.erase(unique(a.begin(), a.end()), a.end());
```

反转

```cpp
reverse(a.begin(), a.end());
```

---

### `pair`

```cpp
pair<int, int> p;
p.first = 3;
p.second = 5;

pair<int, int> q = {1, 2};

cout << q.first << " " << q.second << endl;
```

在 BFS 中很常见：

```cpp
queue<pair<int, int>> q;
q.push({0, 0});
```

---

### `string`

```cpp
string s;
cin >> s;

cout << s.size() << endl;
cout << s[0] << endl;

for (int i = 0; i < s.size(); i++)
    cout << s[i] << " ";
cout << endl;
```

常用操作：

```cpp
s += 'a';
s += "bc";

sort(s.begin(), s.end());
reverse(s.begin(), s.end());
```

---

### `lower_bound` 与 `upper_bound`

要求序列有序。

```cpp
sort(a.begin(), a.end());

int pos1 = lower_bound(a.begin(), a.end(), x) - a.begin();
int pos2 = upper_bound(a.begin(), a.end(), x) - a.begin();
```

`lower_bound`：第一个 `>= x` 的位置

`upper_bound`：第一个 `> x` 的位置

---

### `set` 与 `map`

`set`：自动有序、自动去重。

```cpp
set<int> s;
s.insert(3);
s.insert(1);
s.insert(3);

cout << s.size() << endl;   // 2

if (s.count(1)) cout << "yes\n";
s.erase(1);
```

`map`：键值对映射，自动按 key 有序。

```cpp
map<string, int> mp;
mp["alice"] = 95;
mp["bob"] = 88;

cout << mp["alice"] << endl;

if (mp.count("cat") == 0) cout << "not found\n";
```

遍历：

```cpp
for (auto it = mp.begin(); it != mp.end(); it++)
    cout << it->first << " " << it->second << endl;
```

只想要哈希表效果，也可以用 `unordered_map` / `unordered_set`

## 5. 基础数据结构

### 5.1 栈

后进先出

#### STL 中 `stack` 的基本用法

定义

```cpp
stack<int> st;
```

语法

```cpp
st.push(3);         // 入栈
st.push(5);

cout << st.top() << endl;   // 访问栈顶，当前是 5

st.pop();           // 弹出栈顶

cout << st.top() << endl;   // 当前是 3

cout << st.size() << endl;  // 栈中元素个数
cout << st.empty() << endl; // 是否为空
```

`stack` 只能访问栈顶，不能像数组一样随机访问。

使用 `top()` 前要确保栈非空。

括号匹配

```cpp
string s;
cin >> s;

stack<char> st;
bool ok = true;

for (int i = 0; i < s.size(); i++)
{
    if (s[i] == '(') st.push('(');
    else
    {
        if (st.empty())
        {
            ok = false;
            break;
        }
        st.pop();
    }
}

if (!st.empty()) ok = false;
cout << (ok ? "yes" : "no") << endl;
```

### 5.2 队列

先进先出

应用：BFS 多源最短路 单调队列

#### STL 中 `queue` 的基本用法

定义

```cpp
queue<int> q;
```

语法

```cpp
q.push(10);          // 入队
q.push(20);

cout << q.front() << endl;  // 队头
cout << q.back() << endl;   // 队尾

q.pop();             // 弹出队头

cout << q.front() << endl;
cout << q.size() << endl;
cout << q.empty() << endl;
```

BFS

```cpp
queue<pair<int, int>> q;
q.push({sx, sy});
vis[sx][sy] = 1;

while (!q.empty())
{
    pair<int, int> now = q.front();
    q.pop();

    int x = now.first;
    int y = now.second;

    for (int k = 0; k < 4; k++)
    {
        int nx = x + dx[k];
        int ny = y + dy[k];
        if (合法且没访问过)
        {
            vis[nx][ny] = 1;
            q.push({nx, ny});
        }
    }
}
```

### 5.3 双端队列 `deque`

`deque` 可以头尾都插、都删。

```cpp
deque<int> q;

q.push_back(3);
q.push_front(1);
q.push_back(5);

cout << q.front() << endl;  // 1
cout << q.back() << endl;   // 5

q.pop_front();
q.pop_back();
```

它是单调队列的常用底层容器。

### 5.4 单调队列

`练习4/A.cpp`：Sliding Window

维护滑动窗口最值

```cpp
deque<int> q;

for (int i = 1; i <= n; i++)
{
    while (!q.empty() && q.front() <= i - k)
        q.pop_front();

    while (!q.empty() && a[q.back()] >= a[i])
        q.pop_back();

    q.push_back(i);

    if (i >= k)
        cout << a[q.front()] << " ";
}
```

### 5.5 单调栈

例：求每个元素左边第一个比它大的位置

```cpp
stack<int> st;

for (int i = 1; i <= n; i++)
{
    while (!st.empty() && a[st.top()] <= a[i])
        st.pop();

    if (st.empty()) left_big[i] = 0;
    else left_big[i] = st.top();

    st.push(i);
}
```

## 6. 树、堆、图

### 6.1 二叉树

有一个 **n**(**n**≤10^6) 个结点的二叉树。给出每个结点的两个子结点编号（均不超过 **n**），建立一棵二叉树（根节点的编号为 **1**），如果是叶子结点，则输入 `0 0`。

建好树这棵二叉树之后，依次求出它的前序、中序、后序列遍历

```cpp
struct node
{
    int l, r;
};
vector<node> tree;

void pre_dfs(int root)
{
    if (root == 0)
        return;
    cout << root << " ";
    if (tree[root].l == 0 and tree[root].r == 0)
    {
        return;
    }
    pre_dfs(tree[root].l);
    pre_dfs(tree[root].r);
}

void in_dfs(int root)
{
    if (root == 0)
        return;
    in_dfs(tree[root].l);
    cout << root << " ";
    in_dfs(tree[root].r);
}

void post_dfs(int root)
{
    if (root == 0)
        return;
    post_dfs(tree[root].l);
    post_dfs(tree[root].r);
    cout << root << " ";
}

void solve()
{
    cin >> n;
    tree.push_back({0, 0});
    for (int i = 1; i <= n; i++)
    {
        int a, b;
        cin >> a >> b;
        tree.push_back({a, b});
    }
    pre_dfs(1);
    cout << endl;
    in_dfs(1);
    cout << endl;
    post_dfs(1);
}
```

### 6.2 堆

可快速取最大值 / 最小值的完全二叉树。

应用：优先队列 Top K Dijkstra

#### STL 中 `priority_queue` 的基本用法

默认大根堆：

```cpp
priority_queue<int> q;

q.push(3);
q.push(7);
q.push(1);

cout << q.top() << endl;  // 7
q.pop();
cout << q.top() << endl;  // 3
```

小根堆：

```cpp
priority_queue<int, vector<int>, greater<int>> q;
```

存 `pair`：

```cpp
priority_queue<pair<int, int>> q;
q.push({5, 2});
q.push({3, 7});
```

如果是最短路里常写成：

```cpp
priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> q;
```

表示按 `pair` 的第一关键字从小到大弹出。

### 6.3 图

存储：邻接矩阵，邻接表

遍历：DFS，BFS

## 7. DFS、BFS

### 7.1 DFS 全排列

按照字典序输出自然数 **1** 到 **n** 所有不重复的排列，即 **n** 的全排列，要求所产生的任一数字序列中不允许出现重复的数字。

```cpp
int n;
bool vis[50];  // 访问标记数组
int a[50];     // 排列数组，按顺序储存当前搜索结果

void dfs(int step) {
  if (step == n + 1) {  // 边界
    for (int i = 1; i <= n; i++) {
      cout << setw(5) << a[i];  // 保留5个场宽
    }
    cout << endl;
    return;
  }
  for (int i = 1; i <= n; i++) {
    if (!vis[i]) {  // 判断数字i是否在正在进行的全排列中
      vis[i] = true;
      a[step] = i;
      dfs(step + 1);
      vis[i] = false;  // 这一步不使用该数 置0后允许下一步使用
    }
  }
  return;
}
```

### 7.2 BFS

洛谷P1144 最短路计数

给出一个 **N** 个顶点 **M** 条边的无向无权图，顶点编号为 **1**∼**N**。问从顶点 **1** 开始，到其他每个点的最短路有几条。

```cpp
cin>>n>>m;
for(int i=1;i<=m;i++)
{
	cin>>x>>y;
	e[y].push_back(x);
	e[x].push_back(y);
}
queue<int> q;
q.push(1);
ans[1]=1;
while(!q.empty())
{
	tmp=q.front();
	for(int i=0;i<e[tmp].size();i++)
	{
		if(vis[e[tmp][i]]==0)
		{
			vis[e[tmp][i]]=1;
			q.push(e[tmp][i]);
			dp[e[tmp][i]]=dp[tmp]+1;
		}
		if(dp[e[tmp][i]]==dp[tmp]+1)
		{
			ans[e[tmp][i]]=(ans[tmp]+ans[e[tmp][i]])%100003;
		}
	}
	q.pop();
}
ans[1]=1;
for(int i=1;i<=n;i++)
{
	cout<<ans[i]<<"\n";
}
```

## 8. 数论基础

### 8.1 gcd

最常见性质：

- `gcd(a, b) = gcd(b, a % b)`

```cpp
// Version 1
int gcd(int a, int b) {
  if (b == 0) return a;
  return gcd(b, a % b);
}

// Version 2
int gcd(int a, int b) { return b == 0 ? a : gcd(b, a % b); }
```

### 8.2 快速幂

给你三个整数 **a**,**b**,**p**，求 (a^b) mod p

```cpp
long long binpow(long long a, long long b, long long p) {
  long long res = 1;
  while (b > 0) {
    if (b & 1) res = res * a % p;
    a = a * a % p;
    b >>= 1;
  }
  return res;
}
```

### 8.3 线性筛

```cpp
vector<int> pri;
bool not_prime[N];

void pre(int n) {
  for (int i = 2; i <= n; ++i) {
    if (!not_prime[i]) {
      pri.push_back(i);
    }
    for (int pri_j : pri) {
      if (i * pri_j > n) break;
      not_prime[i * pri_j] = true;
      if (i % pri_j == 0) {
        // i % pri_j == 0
        break;
      }
    }
  }
}
```

## 9. DP

### 9.1 数字三角形

给定一个如下图所示的数字三角形，从顶部出发，在每一结点可以选择移动至其左下方的结点或移动至其右下方的结点，一直走到底层，要求找出一条路径，使路径上的数字的和最大。

Input

第一行包含整数 **n**，表示数字三角形的层数。

接下来 **n** 行，每行包含若干整数，其中第 **i** 行表示数字三角形第 **i** 层包含的整数。

Output

输出一个整数，表示最大的路径数字和。

 Sample Input 1

```
5
7
3 8
8 1 0
2 7 4 4
4 5 2 6 5
```

 Sample Output 1

```
30
```

`dp[i][j]` 表示从 `(i, j)` 出发到底部的最大路径和

状态转移方程：

```cpp
dp[i][j] = max(dp[i + 1][j], dp[i + 1][j + 1]) + a[i][j];
```

### 9.2 最长上升子序列

朴素 DP：`dp[i]` 表示以 `a[i]` 结尾的 LIS 长度

枚举所有 `j < i`，若 `a[j] < a[i]`，则可以转移

复杂度 `O(n^2)`

优化，复杂度O(nlgn)

```cpp
int n, a[MAXN], d[MAXN], di[MAXN], pre[MAXN], res[MAXN];

int dp() {
  int ans = 0;
  for (int i = 1; i <= n; ++i) {
    int tmp = std::upper_bound(d, d + ans, a[i]) - d;
    pre[i] = tmp ? di[tmp - 1] : -1;
    d[tmp] = a[i];
    di[tmp] = i;
    if (tmp == ans) ++ans;
  }
  // Construct the subsequence.
  for (int k = ans, i = di[ans - 1]; k; --k) {
    res[k] = a[i];
    i = pre[i];
  }
  return ans;
}
```

### 9.3 最大子段和

以当前位置结尾的最优值，只和前一个状态有关

常见写法

```cpp
now = max(0LL, now + x);
ans = max(ans, now);
```

## 10. 背包

### 10.1 01 背包

每件物品最多选一次

 `dp[j]` 表示容量为 `j` 时的最大价值

状态转移方程

```cpp
for (int j = V; j >= w; j--)
    dp[j] = max(dp[j], dp[j - w] + c);
```

倒序防止同一件物品被重复使用

例（练习7 D题）

Description

一个旅行者有一个最多能装 **M** 公斤的背包，现在有 **n** 件物品，它们的重量分别是**W**_**1**，**W**_**2**，**.**.**.**,**W**_**n**,它们的价值分别为**C**_**1**,**C**_**2**,**.**.**.**,**C**_**n**，求旅行者能获得最大总价值。

Input

第一行：两个整数，**M**(背包容量，**M**≤**2**0**0**)和**N**(物品数量，**N**≤**3**0)；

第**2**.**.**N**+**1行：每行二个整数**W**_**i**，**C**_**i**，表示每个物品的重量和价值。

Output

仅一行，一个数，表示最大总价值。

 Sample Input 1

```
10 4
2 1
3 3
4 5
7 9

```

 Sample Output 1

```
12
```

```cpp
cin >> m >> n;
    vi dp(m + 1, 0);
    for (int i = 1; i <= n; i++)
    {
        int w, c;
        cin >> w >> c;
        for (int j = m; j >= w; j--)
        {
            dp[j] = max(dp[j], dp[j - w] + c);
        }
    }
    cout << dp[m] << endl;
```

### 11.2 完全背包

每件物品可以选无限次

状态转移方程

```cpp
for (int j = w; j <= V; j++)
    dp[j] = max(dp[j], dp[j - w] + c);
```

### 11.3 分组背包

物品分组,每组最多选一件

处理一组时，要保证上一组结果不被当前组内其他物品重复连锁更新

先备份上一轮 dp，再从上一轮的数据基础上dp

Description

一个旅行者有一个最多能装V公斤的背包，现在有n件物品，它们的重量分别是**W**_**1**，**W**_**2**，**.**.**.**,**W**_**n**，它们的价值分别为**C**_**1**,**C**_**2**,**.**.**.**,**C**_**n**。这些物品被划分为若干组，每组中的物品互相冲突，最多选一件。求解将哪些物品装入背包可使这些物品的费用总和不超过背包容量，且价值总和最大。

Input

第一行：三个整数，V(背包容量，V≤200)，N(物品数量，N≤30)和T(最大组号，T≤10)；

第2..N+1行：每行三个整数**W**_**i**,**C**_**i**,**P**，表示每个物品的重量，价值，所属组号。

Output

仅一行，一个数，表示最大总价值。

 Sample Input 1

```
10 6 3
2 1 1
3 3 1
4 8 2
6 9 2
2 8 3
3 9 3

```

 Sample Output 1

```
20
```

```cpp
    cin >> m >> n >> T;
    vector<vector<pii>> g(T + 1);
    for (int i = 1; i <= n; i++)
    {
        int w, c, p;
        cin >> w >> c >> p;
        g[p].push_back({w, c});
    }

    vector<ll> dp(m + 1, 0);
    for (int i = 1; i <= T; i++)
    {
        vector<ll> pre = dp;
        for (int j = 0; j < g[i].size(); j++)
        {
            int w = g[i][j].first;
            int c = g[i][j].second;
            for (int v = m; v >= w; v--)
            {
                dp[v] = max(dp[v], pre[v - w] + c);
            }
        }
    }
    cout << dp[m] << endl;
```

### 11.4 多重背包

特征：

- 每件物品有个数限制

常见处理方法：

- 直接拆成若干件 0/1 物品
- 二进制优化拆分
