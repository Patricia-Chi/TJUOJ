#include <bits/stdc++.h>
#define ll long long
#define endl '\n'
// #define int long long // 不开 long long 见祖宗
//  #pragma GCC optimize(3)

using namespace std;

const double PI = 3.141592653589793;
int T;
int k;
ll ans;
string s;
int n;

void solve()
{
    // 此题意在锻炼书写循环而不是直接抄答案，因为n=10，四重循环也不会T，推荐模拟一遍查找过程。
    cout << 1634 << endl;
    cout << 8208 << endl;
    cout << 9474 << endl;
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);
    T = 1;
    // cin >> T;
    while (T--)
    {
        solve();
    }
    return 0;
}